#include "hlp.h"

void hlp(const char*, uint32_t *cwd) {
    print("Commands: hlp, cls, say, ver, hi, ls, see, add, rm, mkd, cd, sum, clk, clr, ban, bep, off, res, dly, run, txt, cpy, mve, die, pth, bit, svr, &, mus, dve, mem, box, hey.");
}
